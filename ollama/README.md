# Legacy folder

Haze **no longer uses Ollama** for AI. Configure **`OPENAI_API_KEY`** (and optionally **`ANTHROPIC_API_KEY`**) in `.env.local` / your host environment.

The old `Modelfile` in this folder is unused by the current app. You can delete this directory if you do not need it for other projects.
